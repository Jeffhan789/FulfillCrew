# Deployment

The MVP can be deployed as separate frontend and backend containers.

Suggested next steps:

1. Add PostgreSQL service to `docker-compose.yml`.
2. Replace in-memory product state with database persistence.
3. Add CI checks for backend tests and frontend build.
4. Deploy the backend on a Linux VM or container platform.

